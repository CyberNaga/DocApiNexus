PORT=5000
JWT_SECRET=docapinexus_local_secret_change_later
#Important: this is okay for local learning. Later, never expose real JWT secrets in GitHub.
JWT_EXPIRES_IN=1h

DB_HOST=localhost
DB_PORT=5432
DB_NAME=docapinexusdb
DB_USER=docapiuser
DB_PASSWORD=DocApi@123
# Never expose real database credentials in GitHub. This is okay for local learning. Later, use environment variables or secret management tools to handle sensitive information securely.